#ifndef _truck_
#define _truck_
#include "vehicle.h"
#include <iostream>

template <typename T>
class Link1;

class truck_node : public base
{ 
  private:
     int w_capacity;
     int v_capacity;
     bool status;     
  protected:
     truck_node* link;  
  public:
      truck_node(void);
      ~truck_node(){}
      truck_node(std::string& name, std::string& color,int& mileage,int& top_speed,int& no_gear,int& w_capacity,int& v_capacity);
template <typename T>
friend class Link1;
  };
#endif  
