#ifndef _car_
#define _car_
#include "vehicle.h"
#include <iostream>

template <typename T>
class Link1;
class car_node : public base
{
 private:             
        bool status; 
 protected:
 	car_node* link;          
 public:
        car_node(void);
        ~car_node(){}
        car_node(std::string& name, std::string& color,int& mileage,int& top_speed,int& no_gear);
       
template <typename T>
friend class Link1;
};
#endif
